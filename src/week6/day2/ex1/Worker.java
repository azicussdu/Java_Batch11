package week6.day2.ex1;

public class Worker {
    String name;
    int age;

    // we can use Company class as a property for Worker class
    // so cmpn is a company where the worker is now working
    Company cmpn;
    // objects of a class can be used as a field of another class
}
