package week7.day2.ex2;

/*
    Enum(enumeration) - is a special "class", that represents a group of constants
    (by constants - I mean unchangeable(final))
 */
public enum Genre {
    COMEDY,
    DRAMA,
    ROMANTIC,
    THRILLER
}
